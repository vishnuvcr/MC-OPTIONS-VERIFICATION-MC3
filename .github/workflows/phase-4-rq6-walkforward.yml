name: Phase 4 - RQ6 Walk Forward

on:
  workflow_dispatch:

jobs:
  phase4:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: test -f research/phase4/WALKFORWARD_METHOD_EFFECT.md
